// === auth.service.ts ===
// Service xác thực: đăng nhập (Keycloak + MongoDB), đăng ký tự do (Operator), refresh token, logout, forgot/reset password qua email. Key methods: login, register, logout, refreshToken, forgotPassword, resetPassword, getMe.