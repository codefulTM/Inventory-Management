// === inventory-lot.controller.ts ===
// Controller CRUD lô hàng tồn kho: POST/GET/PUT/DELETE /inventory-lots, /statistics, /expiring-soon, /expired, /samples, /search, /options, /filter, /material/:material_id. Phân quyền: Operator, Manager, QC Technician.