// === dashboard.service.ts ===
// Service dashboard tổng hợp: summary tổng quan (total_quantity, total_value, top_materials), trends (in/out theo ngày/tuần/tháng), drilldown (giao dịch có phân trang). Sử dụng InventoryTransactionRepository, InventoryLotRepository, WarehouseSlipRepository aggregation pipelines.