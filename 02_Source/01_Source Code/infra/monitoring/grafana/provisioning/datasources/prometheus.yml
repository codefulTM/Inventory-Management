apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://monitoring_prometheus:9090
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      scrapeInterval: 5s
      timeInterval: 5s
